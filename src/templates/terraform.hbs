{{> header_generated}}

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "{{terraform.aws_version}}"
    }
  }

  required_version = "{{terraform.terraform_version}}"
}

